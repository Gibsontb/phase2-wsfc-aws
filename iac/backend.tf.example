terraform {
  backend "s3" {
    bucket         = "your-terraform-state-bucket"
    key            = "phase2/prod/terraform.tfstate"
    region         = "us-east-1"
    dynamodb_table = "your-terraform-locks"
    encrypt        = true
  }
}
